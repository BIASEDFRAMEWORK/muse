# Dev Container

This repo can be opened in a VS Code or GitHub Codespaces dev container.

The container provides:

- Node.js 20
- Python 3.12
- GitHub CLI
- `markitdown` installed with `pip --user`

After the container is created, `npm install` runs automatically and dependencies are installed into a container-only `node_modules` volume.
